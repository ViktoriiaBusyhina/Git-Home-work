1.Сгенерировать ssh ключ и добавить его в гитхаб
Создать репозиторий и его адрес 
Добавить его командой 
git clone <ссылка на репозиторий>

2.мы создаем его с помощью команды git init, этой командой мы инициализируем новый локальный репозиторий.
чтобы добавить его в ГитХаб: git add .
git commit -m "Initial commit"
git remote add origin <URL-адрес репозитория>
git push -u origin master

3.git fetch - без слияния
git pull - со слиянием




1-Команда git status отображает состояние рабочего каталога и раздела проиндексированных файлов.
 С ее помощью можно проверить индексацию изменений и увидеть файлы, которые не отслеживаются Git.
2-По умолчанию (без аргументов) git log перечисляет коммиты, 
сделанные в репозитории в обратном к хронологическому порядке — последние коммиты находятся вверху.
3-git remote -v Список ваших удаленных подключений к другим репозиториям c URL-адресом
 каждого подключения. 

